apiVersion: v1
kind: Service
metadata:
  name: simple-java-app
  namespace: default
  labels:
    app: simple-java-app
spec:
  type: ClusterIP
  selector:
    app: simple-java-app
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  sessionAffinity: None
